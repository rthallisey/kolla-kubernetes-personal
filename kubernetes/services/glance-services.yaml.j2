apiVersion: v1
kind: Service
spec:
  ports:
    - port: {{ glance_registry_port }}
  selector:
    name: glance
metadata:
   name: glance-registry

---
apiVersion: v1
kind: Service
spec:
  ports:
    - port: {{ glance_api_port }}
  selector:
    name: glance
metadata:
   name: glance-api